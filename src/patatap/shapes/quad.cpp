#include <patatap/shapes/quad.hpp>

namespace patatap {
  // Quad::Quad(double tPosX, double tPosY, double tWidth, int tAnimationNumber)
  //   : Shape(double tPosX, double tPosY, double tWidth, int tAnimationNumber)
  // {}

  void Quad::InitShape () {
    Shape::InitShape();
  }
}
